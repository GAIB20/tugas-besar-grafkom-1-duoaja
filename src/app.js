import { ShapeManager } from "./models/shapeManager.js";

// Get the canvas element
const canvas = document.getElementById("canvas");

// Init ShapeManager
const shapeManager = new ShapeManager(canvas);